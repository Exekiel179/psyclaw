# 排查报告：锦创健康素养分析会话（2026-08-17）

**项目**：F:\e_work\260425-110352208310969锦创（健康中国背景下大学生健康素养水平及影响因素调查）
**会话**：`psyclaw-session-2026-08-17T07-51-59-867Z_01a00eb4-...`（325 条消息，38 分钟）
**目的**：定位 token 消耗、时间消耗、图片/表格/摘要质量问题。

---

## 1. Token 消耗定位（大量消耗在缓存重读）

| 项 | 数值 |
|---|---|
| 输入 tokens | 193,243 |
| 输出 tokens | 126,357 |
| **缓存读取 tokens** | **11,474,176（1147 万）** |
| 合计费用 | **$0.99**（其中缓存约 $0.80，占 ~92%） |
| 工具调用 | 168 次：**bash 98**、read 28、edit 20、write 17、psyclaw_skill 3、ls 2 |

**根因**：不是某次大读取（无 >20KB 的整读），而是**长会话 + 高密度工具调用导致上下文持续膨胀**：325 条消息里 152 轮带用量，每轮模型都要重读整个缓存上下文（均值约 7.5 万 tokens/轮），累计 1147 万缓存 tokens。

**可避免/可缓解**：
1. **尽早压缩**：本次 325 条消息只有 2 次 compaction——建议上下文超 ~150K tokens 即触发压缩/分段。
2. **bash 输出不进上下文**：98 次 bash 若把 Python 输出重定向到日志文件、用 `read/grep` 只取需要的行，可省大量 token。
3. **分阶段会话**：文献检索/数据分析/论文写作分会话推进，各自只带必要产物，避免单会话上下文无限膨胀。
4. **大 JSON 不整读**：`candidates.json`（180KB）、`verification_ledger.json`（45KB）用 grep 抽标题/摘要，勿整体进上下文。

**费用缓存核对**：pricing 已含 cacheRead/cacheWrite（USD 0.07/0.27，CNY 0.5/2 元每百万），本次 92% 费用来自缓存——面板「缓存」列与实际费用均已正确计入。

## 2. 时间消耗定位（38 分钟）

- 两个 >200s 长间隔：**265s**（数据分析管线运行）、**221s**（导出/DOCX）；另有 7 个 60–180s 工具运行。
- 主要构成：Python 脚本串行执行（run_all.py 顺序 load→prepare→analyze→validate→export，98 次 bash 中大量为脚本往返）+ 126K 输出 token 的写作（论文初稿 30KB）。

**可加快**：
1. 独立分析步骤并行化（analyze 与 validate 可并发）。
2. 写作分节输出（每节 2–4K token），避免一次性生成 30KB。
3. 脚本只打印摘要与校验行，不打印全量数据帧。

## 3. 图片质量问题（简单条形图）

`export.py` 全部 4 张图均为 `ax.bar` 条形图（fig1 维度均分±SD、fig2 因子维度、fig3 组间差异 1×2 子图、fig4 回归系数条）。与"高级图表提升文章质感"的目标不符。

**升级建议**（见图表规范）：
- fig4 回归 → **真正的森林图**（系数 95% CI 横线 + 参照轴）或瀑布图
- 新增 **相关系数热力图**（健康素养维度 × 影响因素维度）
- fig3 组间差异 → **小提琴图 + 蜂群/抖动散点**
- fig1 维度剖面 → **雷达图** 或哑铃/横向 lollipop
- 统一：300DPI PNG + 可编辑 SVG（已具备）、统一配色/字号、数值标注

## 4. 表格问题

- **三线表：DOCX 已成功生成**（tblBorders top/bottom single、insideH/insideV none、表头下框线 31 单元格在）。用户感知"失败"很可能来自**表格过大导致的视觉崩塌**。
- **表格过大**：表1（31 行人口学）、表7（23 行 × 7 列回归）在纵向 A4 溢出/拥挤。需要：表格字号 9pt、宽表横向页面（landscape）或精简类别行。
- **NaN 泄漏（真实 bug）**：表7 常数项 β=`nan`、VIF=`nan`——导出时未清洗 NaN，应显示"—"。
- 面板/报告 md 表格需同样三线样式。

## 5. 摘要学术化

现状摘要为单段式（目的/方法/结果/结论连排），信息完整但缺规范化结构。优化：
- 四段式结构化摘要（目的/方法/结果/结论）
- 结果带数据支撑：均值±SD、率、检验统计量（t/F/r）与 **95%CI / 效应量**
- 结论衔接政策（健康中国 2030）并**明确局限**（单中心、横断面不能推断因果）
- 关键词规范（3–6 个，含主题词与研究方法）

## 6. 流程建议（产品级）

1. **论文/综述重阶段前预告知**：文献检索与核验、全稿写作、DOCX 导出开始前，明确告知预计 token/费用/时间并等待确认。
2. **产出质量清单**（docs/文档规范.md）：图表类型、三线表、摘要结构、NaN 清洗、宽表处理作为交付硬检查。
