# psyclaw 项目规则

开始任何实现前，先阅读 `docs/开工纪要.md`、`docs/架构蓝图.md` 和 `docs/评测框架.md`。它们是本项目的范围、接口和完成标准真源。技能准入细节以 `docs/技能与生态准入清单.md` 为准。

## 不可妥协的边界

- 不 fork 或复制 Pi 的 session/model/skill/plugin runtime；通过 adapter 和 extension 接入锁定版本的官方 Pi。
- 不在 psyclaw 核心实现统计学算法。统计计算委托成熟 Python/R 库、SPSS/Stata/Mplus 或受信任 MCP，并保存可复现脚本与环境信息。
- 不把未经来源核验的引用、Claim、数据、统计数值或审稿意见写成事实。无法验证时标为 `uncertain` 或阻断。
- `data/raw`、凭据、`.git` 和符号链接目标不可写；原始/敏感资料不得未经授权进入外部模型或网络。
- Skill、Plugin、MCP 默认是“发现但不执行”。启用前应记录或展示来源、版本/ref、许可与依赖等信任状态；缺失字段提示并由模型补充，**不把缺字段当成硬阻断**，也**不得把通用信任要求过度解释成「必须由受管安装生成 `psyclaw-install.json` / manifest」**。路径越界、符号链接写入、凭据访问仍拒绝。细则见 `docs/技能与生态准入清单.md`。
- 任何副作用都要有结构化 receipt、审批记录和幂等键；Verifier 不接受模型口头声称作为完成证据。
- 社会科学证据标准按研究范式配置，不把 RCT/Meta-analysis 当作所有研究的唯一金标准。

## 行为约束（防自作主张 / 过度测试 / 过度防御 / 过度发挥）

对齐常见高星仓做法（如 [openai/codex](https://github.com/openai/codex/blob/main/AGENTS.md) 的「先窄测、全量先问」；[happier](https://github.com/happier-dev/happier/blob/dev/AGENTS.md) 的「勿发明硬门槛、做最小相干改动」）：

### 1. 禁止自作主张

- 只做用户当前消息明确要求（或已批准计划内）的事。解释 / 审查 / 诊断 / 规划类请求默认只读汇报，**未经明确要求不得改代码或落盘**。
- 发现相邻 bug、文案、重构机会时：写入最终回复的「可选后续」即可，**不要擅自修**。
- 意图有两种合理解读且会影响产物时：先问一句；不要静默选边并继续。
- 不创建、替换或大幅改写仓库计划 / 文档 / catalog 文案，除非用户点名要改。

### 2. 禁止过度测试

- 默认只跑与改动直接相关的最小验证（例如单个测试文件、`pnpm typecheck` 中与本次相关的部分）。**全量 `pnpm test`、`pnpm eval`、`pnpm eval:modules`、发布向检查须先说明范围并获许可**。
- 不为静态常量、已删除逻辑、纯文案/格式、或「以防万一」的分支堆测试；行为变更才补/改能区分对错实现的最小契约测试。
- 不要为了「证明严谨」反复重跑同一套已通过的检查；不要在 fmt/类型检查后再无故重跑全量测试。

### 3. 禁止过度防御

- 不要把政策句子发明成更硬的门禁（典型错误：把「要有来源/ref/哈希/许可状态」读成「必须受管 install manifest 才能启用」）。
- 不要为臆测的未来调用方、极端边角或「架构上更完整」新增第二套校验路径、双轨状态机或额外 fail-closed 层；先复用现有真源与 choke point。
- 安全边界以已有路径/符号链接/凭据/raw 写拒绝为准；正文预检与缺元数据提示是 advisory，除非代码与准入清单已写明阻断。

### 4. 禁止过度发挥

- 每个改动行都应能回溯到用户请求。无关重构、大规模搬迁、顺手「清理」、扩大 API 面一律不做。
- 做**最小相干**修改：修在现有 owner 上，不平行再造一套；不为一次性调用抽取无复用的小函数。
- 完成一个垂直切片即停；不要扩张到未点名的技能搬运、目录重排或版本 bump。
- 报告里区分：已做 / 未做（需批准）/ 观察到但未动。

## 协作与交付

- 不得自行发布新的大版本；不得 push、打 tag、改 CI/CD 或触碰凭据，除非用户明确授权。
- 复杂任务使用 Orca orchestration 的 Run/Task/Dispatch；每个 worker 只负责一个有边界的任务，先读契约再改代码。
- 并行 worker 首期只允许只读操作；需要写入时使用独立 worktree 或由主执行器串行落盘并验收。
- 在获准的验证范围内，改动应附带实际跑过的命令与输出；跳过的检查要说明原因。
- 不读取、复制或提交任何密钥内容；发现疑似泄露时只记录路径和处置建议。
